 check process policyd with pidfile /var/run/policyd2.pid
   group mail
   start program = "/etc/init.d/postfix-policyd start"
   stop  program = "/etc/init.d/postfix-policyd stop"
   if failed port 10032 protocol postfix-policy within 2 cycles then restart
   if 99 restarts within 99 cycles then timeout
#   depends on policyd_bin
#   depends on policyd_rc
#   depends on cleanup_bin

# check file policyd_bin with path /usr/sbin/postfix-policyd
#   group mail
#   if failed checksum then unmonitor
#   if failed permission 755 then unmonitor
#   if failed uid root then unmonitor
#   if failed gid root then unmonitor

# check file policyd_rc with path /etc/init.d/postfix-policyd
#   group mail
#   if failed checksum then unmonitor
#   if failed permission 755 then unmonitor
#   if failed uid root then unmonitor
#   if failed gid root then unmonitor

# check file cleanup_bin with path /usr/local/policyd/cleanup
#   group mail
#   if failed checksum then unmonitor
#   if failed permission 755 then unmonitor
#   if failed uid root then unmonitor
#   if failed gid root then unmonitor

