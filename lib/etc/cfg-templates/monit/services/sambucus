 check process sambucus with pidfile /var/run/sambucus/sambucus.pid
   group sambucus
   start program = "/etc/init.d/sambucus restart"
   stop  program = "/etc/init.d/sambucus stop"
   if failed unixsocket /var/run/sambucus/sambucus.ctl within 2 cycles then start
   if 99 restarts within 99 cycles then timeout
